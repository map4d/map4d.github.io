# Travel Mode

`MFTravelMode` enum

Định nghĩa các phương thức di chuyển được hỗ trợ

```js
export enum MFTravelMode {
  car = 'car',
  bike = 'bike',
  foot = 'foot',
  motorcycle = 'motorcycle',
}
```
